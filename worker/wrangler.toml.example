name = "jfw-football-data-v2"
main = "index.mjs"
compatibility_date = "2026-08-21"

# Copy this file to worker/wrangler.toml only after the real Cloudflare
# resources exist. Do not commit API_FOOTBALL_KEY; set it with
# `wrangler secret put API_FOOTBALL_KEY` or CI secrets.

[[r2_buckets]]
binding = "FOOTBALL_DATA"
bucket_name = "REPLACE_WITH_R2_BUCKET"

[[d1_databases]]
binding = "FOOTBALL_DB"
database_name = "REPLACE_WITH_D1_DATABASE"
database_id = "REPLACE_WITH_D1_DATABASE_ID"

[vars]
APP_ORIGINS = "https://REPLACE_WITH_PAGES_ORIGIN"
ALLOW_NO_ORIGIN = "false"
SOFT_RATE_LIMIT_PER_MINUTE = "120"
# Comma-separated API-Football competition IDs. Empty means all live fixtures.
LIVE_COMPETITION_IDS = ""
# Phase 3 endpoint flags stay off until the corresponding shadow and
# failure-injection evidence has passed formal review.
D1_DATE_INDEX_ENABLED = "false"
D1_COMPETITION_DATE_INDEX_ENABLED = "false"
# Date-index responses use the Worker's built-in caches.default. No KV/cache
# binding is required; keep both flags false until coverage and review pass.
